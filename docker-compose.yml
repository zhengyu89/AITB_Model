services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: malaysia-landmark-recognition
    ports:
      - "${APP_PORT:-8000}:8000"
    env_file:
      - .env
    gpus:
      - driver: nvidia
        count: all
        capabilities:
          - gpu
    restart: unless-stopped
